---
layout: default
---
<article style="background:hsl({{ date | daysPassed }},var(--saturation),var(--lightness));">
	<div>
		<h1>{{ title }}</h1>
		<time datetime="{{ date | date: "%Y-%m-%d" }}">{{ date | date: "%B %e, %Y" }}</time>
		{{ content }}
	</div>
</article>
