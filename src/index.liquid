---
layout: default
title: "Marcel Appelman"
description: "Tall, skinny Dutch guy with a penchant for skateboarding, cycling, and nerdy computer crap."
pagination:
  data: collections.post
  size: 11
  reverse: true
  alias: posts
---
<div class="md:space-y-40 space-y-20">
  {%- for item in posts -%}
    <article>
      <h1><a href="{{ item.url }}">{{ item.data.title }}</a></h1>
      <time class="text-thc-pop dark:text-thc-pop-dark font-heading text-lg tracking-widest uppercase" datetime="{{ item.date | date: "%Y-%m-%d" }}">{{ item.date | date: "%B %d, %Y" }}</time>
      {%- if item.data.intro -%}
        <p class="lead">{{ item.data.description | safe }}</p>
        <p><a href="{{ item.url }}">(continue reading…)</a></p>
      {%- else -%}
        <p>{{ item.templateContent | safe }}</p>
      {%- endif -%}
    </article>
  {%- endfor -%}
</div>

{% comment %}
<div class="md:text-lg md:mt-40 mt-20 text-sm text-center">
  {% if pagination.href.next %}<a class="text-thc-link hover:bg-thc-hover visited:text-thc-visited dark:text-thc-link-dark dark:hover:bg-thc-hover-dark dark:visited:text-thc-visited-dark" href="{{ pagination.href.next }}">← Older</a>{% endif %}
  {% if pagination.href.previous %}<a class="text-thc-link hover:bg-thc-hover visited:text-thc-visited dark:text-thc-link-dark dark:hover:bg-thc-hover-dark dark:visited:text-thc-visited-dark" href="{{ pagination.href.previous }}">Newer →</a>{% endif %}
</div>
{% endcomment %}